#include <iostream>
using namespace std;

/**
 * @brief bir inorder binary tree örneği
 *
 */

// template <typename Nesne>

// struct Dugum
// {
//     Nesne veri;
//     Dugum<Nesne> *sol;
//     Dugum<Nesne> *sag;
//     Dugum(const Nesne &vr, Dugum<Nesne> *sl = NULL, Dugum<Nesne> *sg = NULL)
//     {
//         veri = vr;
//         sol = sl;
//         sag = sg;
//     }
// };

// void const inOrder(Dugum<Nesne> *altRoot)
// {
//     if (altRoot != NULL)
//     {
//         inOrder(altRoot->sol);
//         cout << altRoot->veri << endl;
//         inOrder(altRoot->sag);
//     }
// }

/**
 * @brief arrayler dinamik bir yapıya sahip değil
 * @note  cin >> n
 * @note  int i[n] KULLANILAMAZ
 */

/**
 * @brief STRUCT
 *
 */

struct ogrenci
{
    long int TC;
    char isim[51];
    float GANO;
    int no;   
};

int main()
{
}